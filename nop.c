#include "monty.h"

/**
 * function_nop - Does nothing.
 * @stack: Double pointer to the head of the stack.
 * @counter: Line number.
 */
void function_nop(stack_t **stack, unsigned int counter)
{
	(void)stack;
	(void)counter;
}
